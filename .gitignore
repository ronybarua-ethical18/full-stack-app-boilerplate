# Dependencies
node_modules
.pnpm-store

# Builds & caches
.next
out
dist
build
.turbo
*.tsbuildinfo

# Env (do not use `.env.*` — that would ignore `.env.example`)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs & debug
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test / coverage
coverage
.nyc_output

# OS & editors
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Deploy / local
.vercel
*.pem

# Backend runtime (if recreated)
**/uploads
.temp
.tmp
